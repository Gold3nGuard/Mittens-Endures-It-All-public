label GrimgrubsPub:
    # This replaces the bar. the convo will actually be TOH trivia
    $ currentLocation = Location.GRIMGRUBS_PUB
    "We arrive at Grimgrubs Pub"

    "Since this location is not implemented yet, we immediately hopped back on Stringbean"
    jump GoToBroom
